# 使用教學：公告面板／每日新番通知／掃描集數快速選取（v25.2.0 起適用）

> **v25.2.0** 新增三項功能：Dashboard 導覽列「排程」按鈕右側新增「**公告**」按鈕；「通知設定」新增「**每日新番通知**」；手動任務「掃描集數」勾選框新增全選／反選／取消與 Shift+拖曳快速選取。
> 這份文件說明這三個功能怎麼用。若想了解本客製版與官方 v24.6 的完整功能差異，請參閱 [README.md](README.md)；只想看 v25.2.0 這次版本的變更清單，請參閱 [RELEASE_NOTES_v25.2.0.md](RELEASE_NOTES_v25.2.0.md)。

---

## 目錄

- [一、公告面板](#一公告面板)
  - [開始之前：這個功能是什麼？](#開始之前這個功能是什麼)
  - [1. 歷史公告](#1-歷史公告)
  - [2. 操作處置](#2-操作處置)
  - [3. 機動調整](#3-機動調整)
- [二、每日新番通知](#二每日新番通知)
- [三、掃描集數快速選取（全選／反選／取消／Shift+拖曳）](#三掃描集數快速選取全選反選取消shift拖曳)
- [四、常見問題 FAQ](#四常見問題-faq)

---

## 一、公告面板

### 開始之前：這個功能是什麼？

「監視公告」（設定 →「其他」區塊的「監視公告」開關）會在每次「更新終了」後偵測動畫瘋首頁的公告文字，判斷是不是在講你追蹤中的作品、以及公告的性質（暫停更新／延後更新／同時更新／暫時下架等），並在「機動調整時間」開關也一併打開時，自動調整這個作品這一週的檢查時間。

v25.1.9 及更早的版本，這些偵測與處置紀錄只寫進程式目錄下的檔案（`gossip_log.jsonl` 等），只能翻程式日誌查看，也沒有地方可以人工覆蓋系統的判斷。v25.2.0 起這些資料改存進 `aniGamer.db`，並在 Dashboard 新增「**公告**」面板（導覽列「排程」右側），一次滿足「查看歷史」「調整處置」「確認目前生效中的排程」三個需求。

> ⚠️ 面板裡看到的資料前提是「監視公告」有開啟；系統自動套用處置的前提是「機動調整時間」也有開啟，否則系統只會記錄公告、不會主動調整排程，「操作處置」下拉選單仍可手動選擇並確認。

---

### 1. 歷史公告

點開「公告」→展開「歷史公告」，捲動清單依時間新到舊列出每一則偵測到的公告：

| 欄位 | 說明 |
|---|---|
| 時間 | 第一次偵測到這則公告的時間（同一則公告持續掛在首頁期間，重複偵測到不會重複列出） |
| 劇名 | 有比對到追蹤中的作品就顯示作品名稱；沒有比對到（例如公告沒有《書名號》標題，或搜尋不到對應作品）就顯示公告原文 |
| 集數 | 比對到作品時，抓取該作品目前線上的最新集數推算：公告內容屬於延後／暫停／下架類，顯示「目前集數 + 1」（下一個受影響的集數）；公告內容有明確列出集數，或屬於同時更新多集，直接採用公告寫的集數；沒有比對到作品則顯示「-」 |
| 狀態 | 暫停更新／提前更新／延後更新／同時更新／暫時下架其中之一；比對到作品時，提前／延後是拿公告寫的新時間跟這部作品過去實際更新的時間比對算出來的，沒有足夠資訊比對（例如全新追蹤、從未下載過）預設判斷為延後更新；沒有比對到作品，或公告內容無法辨識性質，顯示「無法判斷」 |

這個清單每 10 秒自動刷新一次（跟「操作處置」「機動調整」共用同一個刷新排程），開著面板也能看到最新偵測結果。

---

### 2. 操作處置

展開「操作處置」，只列出「目標時間還沒過去」或「無法判斷、需要人工確認」**且尚未實際完成下載**的公告——已經過了目標時間、或該公告要求的更新其實已經抓到並下載完成的項目，都不會再出現在這裡（但仍留在上方「歷史公告」的永久紀錄裡），不需要再手動確認一次。

每一列顯示：

1. **公告時間**與**作品/內容**（含公告原文）。
2. **處置方式**下拉選單，選項為：等待處置／本週暫停更新／本週提前更新／本週延後更新／本週同時更新／自訂更新時間／自訂連續更新時間／忽略此公告／本週下架。系統已依「歷史公告」判斷出的狀態自動選好對應的預設處置（例如狀態是「延後更新」就預設選「本週延後更新」）；選到「**自訂更新時間**」或「**自訂連續更新時間**」時，下方會多出日期／時間（／集數）欄位讓你手動填寫。
3. **確認**按鈕：按下後才會真正套用這個處置（若之前系統或你自己已經套用過別的處置，會先取消舊的再套用新的，不會疊加出兩筆互相矛盾的排程調整）。確認過的列會顯示「已手動確認」徽章，且**下次系統重新檢查或程式重啟都不會再更動這則公告的處置**——這個保護只影響這一則公告本身，不影響同一部作品未來收到的新公告。

若某則公告目前顯示「等待處置」且遲遲無法比對到作品（例如公告標題跟 sn_list.txt 裡的名稱差異太大），可以選「**忽略此公告**」讓它從這個清單消失，不用等它自然過期。

---

### 3. 機動調整

展開「機動調整」，唯讀列出目前生效中的臨時排程調整（暫停檢查、或覆蓋成指定時間檢查／預期集數與目前已找到集數），跟「操作處置」按下確認後產生的結果同步。這裡純粹給你確認「機動調整時間」實際上排了哪些調整，不含任何操作按鈕；調整完成或過期後會自動從這裡移除。

---

## 二、每日新番通知

「設定」→「通知設定」→「通知類別」新增「**每日新番通知**」開關（預設開啟），每天 **00:00** 自動發送一則通知，彙整當天的更新狀況，不用自己記今天有哪些番劇要更新、機動調整又動了哪些排程。

通知內容分兩段：

| 段落 | 內容 | 來源 |
|---|---|---|
| 本日排程更新 | 列出今天（依 `sn_list.txt` 的 `*星期* $HH:MM$` 自訂排程標籤）會檢查更新的作品與時間，並嘗試線上查詢目前實際集數，附上「預計播出第 N 話」；沒有排定項目時顯示「今日無排定更新」 | `sn_list.txt` 自訂排程項目 |
| 機動調整異動 | 列出「監視公告」機動調整今天生效中的暫停／延期／加更等異動；沒有異動時顯示「今日無機動調整異動」 | 需開啟「監視公告」與「機動調整時間」才會有內容，見上方[公告面板](#一公告面板) |

> ⚠️ 只查詢 `sn_list.txt` 有加上自訂星期／時間排程標籤（見 [README「`sn_list.txt` 排程功能強化」](README.md#sn-list-schedule)）的項目；沒有加標籤、走一般輪詢檢查的項目不會出現在「本日排程更新」清單裡，因為輪詢項目本來就沒有固定的每日更新時段可以彙整。

跟其他通知類別一樣，可以在「通知設定」→「通知模板」自訂實際發送的文字內容與格式（Telegram 可用 `<blockquote expandable>` 可摺疊引用標籤，包住的內容達 4 行以上 Telegram 才會顯示收合箭頭，不足 4 行只會顯示成一般引用），也可以到「查看發送歷史」查詢過去每天實際發送過的內容。

---

## 三、掃描集數快速選取（全選／反選／取消／Shift+拖曳）

手動新增任務輸入連結後按「掃描集數」，「本篇」／「中文配音」／「特別篇」三個分類各自的標題旁新增三顆按鈕：

| 按鈕 | 作用 |
|---|---|
| 全選 | 勾選**這個分類底下**全部集數 |
| 反選 | 把這個分類底下每一格目前的勾選狀態反過來（有勾的取消、沒勾的勾上） |
| 取消 | 取消勾選這個分類底下全部集數 |

三顆按鈕都只作用在按鈕所屬的分類，不會影響其他分類已經勾選的狀態（例如按「本篇」的「全選」，不會連帶勾選「特別篇」）。

另外新增 **Shift+拖曳快速選取**：在任一勾選框上按住 `Shift` 並按下滑鼠左鍵，程式會依這一格「目前的狀態」決定接下來要「勾選」還是「取消勾選」（原本沒勾就變成一路勾選、原本已勾就變成一路取消），接著在按住左鍵的狀態下拖曳滑過其他勾選框，滑過的每一格都會套用同樣的結果，放開左鍵即結束。適合只想跳過中間零星幾集（例如整段特別篇只想排除其中 3 集）時，不用逐一點擊或全選後再一個個手動取消。

---

## 四、常見問題 FAQ

**Q：為什麼「操作處置」是空的？**
A：可能目前沒有任何尚未過目標時間、且尚未完成下載的公告，或是「監視公告」開關本身是關閉的（面板一樣看得到過去的歷史紀錄，但不會有新公告產生）。

**Q：我手動選的處置方式，會不會被系統之後又蓋回去？**
A：不會。按下「確認」後這則公告的處置就鎖定了，之後系統的自動判斷不會再覆蓋這一則；但如果同一部作品之後又收到「新的」公告，那則新公告仍會照系統的判斷產生新的預設處置，不受這次手動確認影響。

**Q：「集數」欄位顯示「-」是什麼意思？**
A：代表這則公告沒有比對到目前追蹤中的作品（sn_list.txt 裡沒有這部作品，或搜尋比對失敗），沒有集數資料可以推算。

**Q：「每日新番通知」為什麼沒收到？**
A：先確認「通知類別」的「每日新番通知」開關是否開啟，以及 Telegram／Discord 本身的通知總開關與 Token/Webhook 是否已設定成功（可用「發送測試訊息」驗證）；另外這則通知固定在每天 00:00 觸發一次，若程式在 00:00 當下沒有在執行（例如剛好重啟中），會等到隔天 00:00 才會補發，不會補發錯過的那一天。

**Q：Shift+拖曳選取為什麼沒反應？**
A：確認是先按住 `Shift` 再按下滑鼠左鍵（不是先點擊再按 Shift），且拖曳全程都要按住左鍵；放開左鍵或滑鼠移出瀏覽器視窗都會結束這次拖曳選取。

---

如需回報問題或提出建議，歡迎至 GitHub Issues 留言。
