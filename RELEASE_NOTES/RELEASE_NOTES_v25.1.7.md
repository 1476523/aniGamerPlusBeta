## v25.1.7

延續 v25.1.6，本次修正手動任務的併發控制與 cookie 失效處理，並改善手動批次下載（下載全部/掃描集數後指定範圍）的進度顯示、失敗重試、終止與救援機制，<br>
讓排程下載能在手動任務進行中插隊優先處理，新增程式啟動時自動清理殘留暫存檔，另外美化了設定頁與登入頁。<br>
共 17 項變更。

<details>
<summary><strong><u>🆕 新功能（7 項）</u></strong></summary>

<details>
<summary><strong>手動批次下載進度視窗與清單顯示標題化</strong></summary>

- 手動「下載全部」／「掃描集數」後勾選指定範圍下載時，送出後會跳出進度視窗，<br>
列出這批要下載的所有集數（顯示掃描到的番劇名稱與集數，不再只顯示 sn 數字）、個別即時進度，以及整體進度條；<br>
「重新檢查所有排程更新」的下載清單也一併改為顯示標題而非 sn

</details>

<details>
<summary><strong>失敗清單與個別/整批重試按鈕</strong></summary>

- 過去單一集數重試 3 次仍失敗就會直接從進度列表消失、且無法再次嘗試，作品集數一多、中間幾集連續踩到裝置驗證異常等暫時性錯誤就容易被永久放棄且不易察覺。<br>
現在重試 3 次後最終放棄的集數會額外列在下載清單下方的「失敗清單」中（同樣可捲動），<br>
每一項名稱後方都有獨立的「重試」按鈕（只重新嘗試那一集），旁邊另有「重試所有失敗項目」按鈕可一次重試整份失敗清單；<br>
主清單維持顯示狀態文字，不受影響，仍在自動重試中（狀態顯示「失敗! 重啓中」）的集數不會被算進失敗清單

</details>

<details>
<summary><strong>「顯示目前工作」按鈕，重新叫出批次任務進度畫面</strong></summary>

- 新增「顯示目前工作」按鈕（「手動添加任務」視窗內，提交按鈕左側，<br>
只在有批次任務正在進行時才會出現）：不論是 `aniGamerPlus.exe` 意外關閉後重啟自動續跑、瀏覽器分頁意外關閉、還是不小心點掉了進度視窗，都能透過這顆按鈕重新叫出目前手動批次任務的進度畫面

</details>

<details>
<summary><strong>「終止任務」按鈕</strong></summary>

- 新增「終止任務」按鈕（進度視窗內，關閉按鈕左側）：不會強制中斷正在下載中的那一集（避免中途砍斷連線/寫檔造成半殘檔案），<br>
已經在下載中的集數會自然跑完並正常標記完成或失敗，尚未開始下載的集數會被標記為「已取消」，不提供重試

</details>

<details>
<summary><strong>手動批次任務期間，排程下載可插隊優先處理</strong></summary>

- 手動批次任務進行期間，若排程檢查（含自訂排程、「重新檢查所有排程更新」）偵測到新更新，現在會優先處理：目前正在下載中的那一集手動任務讓它下載完，<br>
但尚未開始下載的集數會先暫停（顯示「已暫停(排程下載優先處理中)」），把並發名額讓給排程下載，處理完後手動任務自動恢復。<br>
過程中網頁會跳出「偵測到排程更新，優先處理中」的提示，這個提示沒有任何關閉方式（不能點掉、按 Esc 或點畫面外側都不會消失），會在排程下載處理完成後自動收起

</details>

<details>
<summary><strong>程式啟動時自動清理殘留暫存檔</strong></summary>

- 下載途中若遇到意外中斷（當機/斷電/被強制關閉，尤其是還沒有目前這套重試機制的舊版本），`temp` 目錄下會留下當時尚未清理的殘留：下載到一半的 `<sn>-downloading-by-aniGamerPlus` 資料夾（m3u8/加密金鑰/分段暫存），<br>
以及合併前後、檔名帶有 `.MERGING.`／`.DOWNLOADING.` 的暫存影片檔。<br>
現在程式每次啟動時都會自動清理這類殘留。<br>
由於 `temp_dir` 可被使用者自訂成任意現存目錄，這裡刻意只刪除認得出來、確定是 aniGamerPlus 自己建立的殘留物（依上述命名規則判斷），<br>
不會整個清空 `temp_dir`，避免萬一該設定被指向其他目錄時誤刪不相干的檔案

</details>

<details>
<summary><strong>下載目錄／暫存目錄新增「瀏覽...」目錄選擇視窗</strong></summary>

- 「下載目錄」「暫存目錄」輸入框右側新增「瀏覽...」按鈕：點擊會開啟一個目錄選擇視窗，<br>
逐層點擊瀏覽伺服器上(執行 aniGamerPlus.exe 那台機器)的資料夾並選定路徑寫回輸入框(瀏覽器基於安全性無法直接跳出原生資料夾選擇視窗讀取伺服器端路徑，所以採用這種常見於自架網頁應用程式的做法)。<br>
開啟時會直接嘗試前往輸入框目前已填的路徑，若該路徑還不存在(例如尚未建立過的下載目錄)，<br>
會自動往上找最近一個確實存在的上層目錄開始顯示，並用溫和的提示(「已設定的目錄不存在」)告知，不會跳出嚇人的技術性錯誤訊息；<br>
為了安全，系統磁碟機(通常是 `C:\`)不開放列出完整目錄樹(避免看到 Windows/Program Files 等系統資料夾，<br>
尤其 Dashboard 可能開放外部存取)，最上層畫面只提供「桌面」「下載」兩個常見安裝位置的捷徑與其他磁碟機，不顯示 `C:\` 本身；<br>
即使 aniGamerPlus 剛好直接安裝在 `C:\` 或其他系統磁碟機根目錄下，往上尋找存在目錄時也不會不小心繞過這個限制、暴露完整系統磁碟機目錄樹；<br>
瀏覽時會自動過濾隱藏/系統資料夾(例如 `$RECYCLE.BIN`、`System Volume Information`)；<br>
磁碟機根目錄(例如 `D:\`)也補上「上一層」可以回到最上層畫面

</details>

</details>

<details>
<summary><strong><u>🐛 BUG 修正（6 項）</u></strong></summary>

<details>
<summary><strong>手動任務未沿用全域並發號誌，不受下載冷卻與並發數限制</strong></summary>

- 修正手動任務（Web 控制臺「下載全部」「掃描集數」後送出的批次/單集任務、以及程式啟動時還原上次意外中斷的手動任務）會用使用者於表單挑選的執行緒數重建全域下載並發號誌，使手動任務不受設定檔「下載冷卻時間(秒)」與「最大並發下載數」限制、並讓正在進行中的排程下載執行緒失去正確的並發數統計；<br>
改為手動任務一律沿用程式啟動時依設定檔建立的全域並發號誌，與排程自動下載共用同一組「最大並發下載數」與「下載冷卻時間」限制。<br>
獨立的命令列一次性執行（`--thread_limit` 參數）不受影響，仍可自訂執行緒數

</details>

<details>
<summary><strong>Cookie 刷新搶號落敗的執行緒誤刪 cookie.txt，導致其他任務失敗</strong></summary>

- 修正高併發情境下多個執行緒同時搶登入 cookie 刷新名額、其中落敗的執行緒會把 `cookie.txt` 整個更名為 `invalid_cookie.txt` 移走，<br>
導致其他仍在下載中的執行緒存取 `cookie.txt`（例如記錄修改時間用於除錯日誌）時遇到「系統找不到指定的檔案」而直接任務失敗的問題：這類「搶刷新名額落敗」情況改為只重置記憶體中的 cookie 快取、保留原始檔案不刪除，讓後續請求可重新讀取同一份 cookie 繼續嘗試；<br>
`cookie.txt` 內容本身確定損毀（空白檔案）時仍會照舊更名隔離

</details>

<details>
<summary><strong>任務頁「添加手動任務」彈窗是舊版複製品，功能與設定頁不同步</strong></summary>

- 修正「任務」頁（`monitor.html`）的「添加手動任務」彈窗是一份從未同步更新的舊版複製品：缺少「更改名稱」欄位、「掃描集數」按鈕與「顯示目前工作」救援按鈕，<br>
且提交邏輯是獨立寫死的舊版 JS（呼叫 `readManualConfig()` 等函式與「設定」頁各自維護一份），送出批次下載也不會跳出進度視窗。<br>
改為與「設定」頁共用同一份 `aniGamerPlus.js`，兩個入口功能完全一致；<br>
同時修正共用的 `renderJson()` 在非完整設定頁（如任務頁）缺少對應欄位時會整個中斷執行的問題

</details>

<details>
<summary><strong>排隊等待並發名額期間，番劇名稱顯示被 sn 佔位字串蓋掉</strong></summary>

- 修正排隊等待並發名額期間，番劇名稱＋集數的顯示會被底層「已提交, 排隊中」階段的 sn 佔位字串蓋掉、要等真正開始下載才會換回正確名稱的問題

</details>

<details>
<summary><strong>其他集數下載中點擊重試成功後，網頁誤將整批標記為已完成</strong></summary>

- 修正在其他集數仍在下載中時點擊重試，該集重試成功後網頁會錯誤地把整個批次標記為已完成、停止更新進度，導致畫面卡在舊狀態、看不到其餘仍在下載中集數後續進度的問題

</details>

<details>
<summary><strong>批次任務中斷後重啟還原會重複下載已完成的集數</strong></summary>

- 修正批次類手動任務（下載全部/指定範圍）意外中斷（當機/斷電/被強制關閉）後重啟還原時，<br>
會整批重新從頭下載、當機前已下載成功的集數也會被重複下載一次的問題：現在每一集下載成功後就會即時記錄進 `manual_batch_tasks.json`，<br>
重啟還原時會跳過已經記錄下載成功的集數，只重新嘗試當機當下仍在下載中或已放棄的集數

</details>

</details>

<details>
<summary><strong><u>🔧 其他改善（3 項）</u></strong></summary>

<details>
<summary><strong>進度視窗不會因誤觸畫面外側或按 Esc 而意外關閉</strong></summary>

- 進度視窗也改成跟「重新檢查所有排程更新」一樣，不會因為誤觸畫面外側或按 Esc 而意外關閉，需明確按下關閉鈕才會收合

</details>

<details>
<summary><strong>設定頁區塊改為可摺疊收納</strong></summary>

- 「代理設定」「通知設定」「其他」三個區塊改成可摺疊收納，預設收合，點擊標題即可展開/收合，畫面更精簡；<br>
「路徑設定」「下載設定」維持展開不變。<br>
「通知設定」裡的 Telegram 通知／Discord 通知也各自再包一層可摺疊子區塊，方便未來新增更多通知方式時不會讓這個區塊越長越長

</details>

<details>
<summary><strong>導覽列文字更名與品牌風格統一</strong></summary>

- 導覽列文字更名：「aniGamerPlus控制臺」→「aniGamerPlus 客製強化版 控制臺」、「自動模式設定」→「設定」、「任務監控中心」→「任務」、「sn_list」→「排程」、「手動添加任務」彈窗標題→「添加手動任務」；<br>
導覽列品牌名稱改成跟登入頁一樣的「主標題+副標題」兩行字型設計(顏色/粗細也與登入頁的「登入」標題統一)，「登出」改成跟「添加手動任務」同樣的按鈕樣式，改用紅色；<br>
登入頁上方新增置中的「aniGamerPlus / 客製強化版 控制臺」品牌標題，並縮小與下方登入表單間的留白

</details>

</details>

<details>
<summary><strong><u>📦 版本（1 項）</u></strong></summary>

<details>
<summary><strong>版本號由 v25.1.6 調整為 v25.1.7</strong></summary>

- 版本號由 v25.1.6 調整為 v25.1.7

</details>

</details>

---

> 完整變更歷史（v24.6 → v25.1.6）請參閱先前的 release notes。
> 
> 如果喜歡的可以按下 Star 來表示喜歡<br>
> 可以給予咖啡表示支持<br>
> <a href="https://www.buymeacoffee.com/tocsh" target="_blank"><img src="https://cdn.buymeacoffee.com/buttons/v2/default-blue.png" alt="Buy SH A Coffee" height="36" width="120"></a>
