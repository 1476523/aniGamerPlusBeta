# 使用教學：Telegram 通知重試機制（v25.2.3 起適用）

> **v25.2.3** 新增 Telegram 通知遇到 Bad Gateway 時的自動／手動重試機制。<br>
> 這份文件說明這項功能怎麼運作、怎麼觸發。<br>
> 若想了解本客製版與官方 v24.6 的完整功能差異，請參閱 [README.md](README.md)；<br>
> 只想看 v25.2.3 這次版本的變更清單，請參閱 [RELEASE_NOTES_v25.2.3.md](RELEASE_NOTES_v25.2.3.md)。

---

## 目錄

- [一、Telegram 通知 Bad Gateway 自動重試](#一telegram-通知-bad-gateway-自動重試)
- [二、通知發送歷史「手動重試」按鈕](#二通知發送歷史手動重試按鈕)
- [三、常見問題 FAQ](#三常見問題-faq)

---

## 一、Telegram 通知 Bad Gateway 自動重試

Telegram 伺服器端偶爾會回應 502 Bad Gateway（多半是 Telegram 那邊短暫不穩，跟 Bot Token／聊天室 ID 設定是否正確無關）。<br>
過去只要遇到這個情況，不管是「每日新番通知」還是下載完成／失敗等其他系統通知，都會直接判定失敗，整則通知就送不出去。

現在只要偵測到失敗原因是「Bad Gateway」，會自動等待 3 秒後重試，最多重試 3 次；3 次都還是失敗才會記入發送失敗。<br>
這個機制全程背景自動運作，不需要、也沒有對應的開關。

> ⚠️ 只有「Bad Gateway」這個特定失敗原因才會自動重試。Token 錯誤、聊天室 ID 未設定等其他失敗原因重試沒有意義，不會觸發自動重試，會直接記入失敗。

## 二、通知發送歷史「手動重試」按鈕

如果自動重試 3 次後仍然失敗，前往「設定」→「通知」區塊 → Telegram「發送歷史」，找到那筆失敗紀錄，<br>
錯誤訊息（結尾會標註「已自動重試 3 次仍失敗」）右側會出現「手動重試」按鈕。

按下按鈕會用該筆紀錄當初的原始內容再送一次，**只會重試 1 次**，不會再自動連續重試；結果（成功或失敗）會另外彈窗顯示，並多記一筆新的發送紀錄，原本那筆失敗紀錄不會被覆蓋或刪除。

> ⚠️ 目前手動重試只支援 Telegram，Discord 的發送歷史沒有存標題內容（Discord embed 標題是另外組成的），暫不支援。

## 三、常見問題 FAQ

**Q：自動重試會不會讓通知延遲很久才送到？**<br>
A：只有真的遇到 Bad Gateway 才會延遲，最多延遲 3 × 3 秒 = 9 秒（3 次重試、每次間隔 3 秒）；沒遇到 Bad Gateway 完全不受影響。

**Q：手動重試按鈕一直沒出現，是不是壞了？**<br>
A：按鈕只會在「Telegram 發送歷史裡有一筆失敗紀錄、且失敗原因包含 Bad Gateway」時才會出現，其他失敗原因（例如 Token 打錯）不會顯示這個按鈕，屬正常現象。

---

如需回報問題或提出建議，歡迎至 GitHub Issues 留言。
