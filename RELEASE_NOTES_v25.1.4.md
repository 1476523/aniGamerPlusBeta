## v25.1.4

延續 v25.1.3，本次修繕了早期就存在於專案中、但從未真正接上功能的自訂登入網頁，並完善了早期就寫好、卻始終沒有網頁設定介面的 Discord／Telegram 通知功能；另外新增「取得當前新番更新時間」與「資料庫整頓」兩個排程輔助功能，並移除已停止維護的 Plex 自動更新／命名功能。同時修正重新採集 cookie/ja3/akamai 後可能出現的裝置驗證異常（code=1007），並在 Dashboard 新增可直接編輯 cookie 的欄位。另外新增手動任務的暫存紀錄機制（程式意外中斷後可自動還原）與「掃描集數」勾選下載功能，並修正多集並發下載時第二集起出現裝置驗證異常（code=1007）的問題。共 22 項變更。

<details>
<summary><strong><u>🔐 Web 控制面板：新功能（6 項）</u></strong></summary>

- **修繕早期網頁文件：自訂登入頁**——`login.html`／`register.html` 原本是專案裡從未被任何路由接上的早期遺留文件，密碼保護實際上一直靠瀏覽器原生的 HTTP Basic Auth 彈出視窗。這次把 `login.html` 真正接上功能，改為自訂登入頁（伺服器端 session 保存，簽章金鑰存成根目錄 `dashboard_secret.key`，重啟不會強制登出）；`register.html` 因與現有密碼欄位設計重複已移除；沿用既有的 `dashboard.BasicAuth`／`username`／`password` 設定。同時修正 `login.html` 引用了一個從未存在過的 jQuery 檔案導致登入頁完全無法運作的問題
- **完善早期代碼：Discord／Telegram 通知設定頁**——`telebot_notify`／`discord_notify` 推送邏輯本來就是早期寫好的功能，但 Dashboard 網頁一直沒有對應設定欄位，只能手動編輯 `config.json`。這次在設定頁補上「通知設定」區塊，可直接開關並填寫 Telegram（Bot Token、Chat ID）與 Discord（Webhook URL）；Token／Webhook 等敏感欄位採遮罩顯示，不會在網頁明文回顯
- **Dashboard 新增 Cookie 編輯欄位**——過去更換 cookie 只能手動編輯根目錄的 `cookie.txt`，這次在設定頁新增 Cookie 欄位可直接貼上保存，同樣採遮罩顯示；保存後會自動觸發下方「裝置ID過期自動偵測」機制，一併重新申請匹配的裝置ID
- **新增「讀取其他版本設定檔」**——可選擇一份其他份 `config.json`（例如舊版備份），程式會比對出跟目前設定不同、且網頁上有對應欄位的項目逐一列出，預設**不勾選**，需自行勾選要匯入的項目才會生效；比對範圍涵蓋 `dashboard`／`ftp`／`user_command` 等原本只能手動編輯的項目，敏感欄位一律遮罩顯示，會影響 Dashboard 連線/登入方式的項目會加上 ⚠ 二次確認
- **取得當前新番更新時間**——「其他」區塊新增按鈕，依序查詢 sn_list.txt 每個項目最新 3 集的上架時間換算成星期＋時間供參考；請求強制間隔 3 秒以上，執行期間暫停自動檢查與新任務排入，完成後可對每部作品自由選擇要套用的候選時段、自訂或跳過（預設跳過），只會改動排程標籤
- **資料庫整頓**——「其他」區塊新增按鈕，重新線上解析 sn_list.txt 各作品名稱，清除 `aniGamer.db` 中不再被追蹤的下載紀錄（不影響已下載的實體檔案）；若過程中有任何項目查詢失敗，為避免誤刪會整個中止不做任何刪除

</details>

<details>
<summary><strong><u>🛟 手動任務強化：暫存紀錄與掃描集數（2 項）</u></strong></summary>

- **暫存紀錄，程式意外中斷後可自動還原**——過去手動新增的任務只存在記憶體裡，程式當機、斷電或被強制關閉就會悄悄消失。新增 `manual_single_tasks.json`／`manual_batch_tasks.json` 兩份暫存紀錄檔，提交時寫入、任務終結時移除；程式啟動時會自動讀取並重新提交上次意外中斷的任務（批次類任務是整批重新送出，當機前已下載完成的集數可能會被重新下載一次，是已知的取捨）
- **「掃描集數」，可勾選指定集數下載**——手動新增任務視窗新增按鈕，對填寫的連結線上查詢完整集數清單，列成勾選框逐集挑選（取代原本下拉選單的下載模式）；集數依「本篇」／「中文配音」／「特別篇」分區顯示、依位數動態補零並正確排序，整數集若同時存在對應半集會改顯示為「24.0」以便對齊；執行期間會暫停自動檢查與新任務排入

</details>

<details>
<summary><strong><u>🧭 Web 控制面板：體驗與效能優化（5 項）</u></strong></summary>

- **任務監控頁導覽列補齊**——補上與「自動模式設定」頁一致的 sn_list、添加手動任務項目；修正一個打開不存在彈窗的死連結；修正導覽列字體大小比例跟其他頁不一致的問題（`layui.css` 與 `bootstrap.rfs.min.css` 載入順序調整）
- **新增手動任務後監控列表延遲，容易誤以為沒提交成功**——過去任務要等並發號誌、SN 解析、且經過解析冷卻時間後才會出現在監控列表，容易讓使用者重複點擊提交。現在任務一提交就先以「已提交, 排隊中」等暫定狀態出現，之後才由實際解析結果覆蓋；排程自動檢查的 `worker()` 存在同樣問題一併修正
- **登出穩定性與 Dashboard 快取破壞**——修正透過反向代理／Cloudflare Tunnel 對外開放時，登出請求被中間層擋下導致「按了沒反應」的問題（改成只有請求真正成功才跳轉）；CSS／JS 資源網址加上版本號查詢字串，版本更新時瀏覽器與邊緣節點會自動失效舊快取；`/static/` 底下所有回應加上 `Cache-Control: no-cache`，確保版號未變動時（例如同版本內連續修正）也不會沿用過期快取
- **大量下載時開啟 Dashboard 網頁可能卡死、變成白畫面**——`before_request` 過去不論請求類型一律先讀取解析 `config.json` 才判斷是否需要驗證登入，下載中磁碟 I/O 競爭激烈時會被嚴重拖慢。已調整為靜態資源與登入頁一律不需要驗證，直接跳過設定檔讀取
- **下載期間 Dashboard 網頁反應變慢、切換頁面白畫面轉圈**——`curl_cffi` 底層 libcurl 的資料接收階段不會主動讓出控制權給 gevent 事件迴圈，多部影片高並發下載時會排擠 Dashboard 自己的網頁請求。已將所有 `curl_cffi` 請求呼叫改丟進 gevent 專屬執行緒池執行，事件迴圈可正常處理網頁請求

</details>

<details>
<summary><strong><u>🛡️ 反爬蟲與裝置驗證穩定性（3 項）</u></strong></summary>

- **ja3/akamai 自動相容性檢測與修正**——新版瀏覽器 TLS 指紋可能帶有 `curl_cffi` 尚未支援切換的擴展編號，直接貼上過去會導致連線崩潰。新增 `sanitize_ja3()` 精準揪出會崩潰的擴展編號並自動移除，於程式啟動與 Dashboard 存檔時檢測；請求層再加一層防護，遇到當下沒偵測到的問題會自動退回內建指紋重試
- **裝置ID過期自動偵測，修正重新採集指紋後的裝置驗證異常**——使用者重新登入／用新瀏覽器重新採集 cookie 與 ja3/akamai 後，`device_id.txt` 仍沿用舊身分，導致新指紋與舊裝置ID對不上被判定異常（`code=1007`）。新增偵測：`cookie.txt` 修改時間比 `device_id.txt` 新即視為裝置ID也已過期，自動重新申請一組
- **裝置ID一旦觸發過一次裝置驗證異常，之後會持續失敗（根因與修正）**——排查證實：只要有一集觸發過一次 `code=1007`，這組 device_id 就會被伺服器標記，之後不管重試幾次、甚至重開程式沿用同一組都會持續失敗，必須換一組全新的裝置ID才能恢復。已修正為偵測到 `code=1007` 時立即清除目前裝置ID並拋出可重試的例外，讓既有重試機制在下一次嘗試時自動申請全新裝置ID；同時修正了另一個會擋住這個重試路徑的既有 bug（`__get_episode_list()` 每次呼叫都往舊資料累加而非清空重建，導致 `renew()` 重試時集數判斷邏輯算錯索引直接崩潰），確保「自動換裝置ID重試」真正能夠生效；並加上裝置身分診斷紀錄方便日後比對排查

</details>

<details>
<summary><strong><u>🐛 BUG 修正（4 項）</u></strong></summary>

- **舊設定檔案不會補完新欄位，導致 Dashboard 崩潰**——`config.json` 版本升級機制依賴的 `latest_config_version` 長期停留在 `17.4` 沒有更新，導致經歷過 v20 前後升級的既有使用者，之後所有新增欄位（`ja3`／`akamai`／`dashboard` 等）都不會再被補進設定檔，Dashboard 讀取設定頁時直接 `KeyError` 崩潰。已調整為 `17.5` 讓這類設定檔正確觸發補完機制，既有設定值不受影響
- **偵測不到 Dashboard 資料夾時整支程式直接崩潰**——只複製 `.exe` 忘記一起帶 `Dashboard` 資料夾時，`Config.py` 內部關閉控制台功能重新存檔時，`read_settings()`／`write_settings()` 共用同一個 dict 物件互相污染，導致 `working_dir` 等欄位被誤刪、整支程式 `KeyError` 崩潰。已改為接收外部傳入設定時複製一份而非直接引用
- **`range` 下載模式在特定主控台編碼下會整段崩潰**——輸出字串裡混用了簡體「务」字，在輸出被導向檔案、或系統為傳統中文編碼（cp950/Big5）時 `print()` 會拋出 `UnicodeEncodeError` 導致該批任務中斷；已統一改回繁體「務」
- **中文配音／特別篇集數會被誤歸類到與正篇相同的資料夾**——`__get_bangumi_name()` 去除標題尾端標籤時邏輯過於激進，把集數編號與番劇類型標籤一起去除，導致中配／特別篇失去區分依據跟正篇混在一起；已修正為只去除與集數無關的雜項標籤，保留番劇類型標籤，中配／特別篇現在會下載到獨立資料夾

</details>

<details>
<summary><strong><u>🗑️ 移除功能（1 項）</u></strong></summary>

- 移除 Plex 自動更新媒體庫、Plex 命名規則兩項功能的全部相關程式碼與設定欄位（已停止維護，主流通知平台已收斂到 Discord／Telegram）；升級時會自動清除既有 `config.json` 中殘留的 Plex 欄位

</details>

<details>
<summary><strong><u>📦 版本（1 項）</u></strong></summary>

- 版本號由 v25.1.3 調整為 v25.1.4

</details>

---

> 完整變更歷史（v24.6 → v25.1.3）請參閱先前的 release notes。
