## v25.2.0

延續 v25.1.9，本次新增客製強化版獨有的「公告」面板：導覽列「排程」右側新增「公告」按鈕，<br>
可查看歷史公告、對每則公告手動指定或覆蓋處置方式、查看目前生效中的機動調整排程，取代過去只能看程式碼日誌、無法在網頁上操作的問題；<br>
新增「每日新番通知」，每天 00:00 主動彙整當天排程更新清單與機動調整異動推播一則通知；<br>
「掃描集數」勾選框新增全選／反選／取消按鈕與 Shift+拖曳快速選取。<br>
同時把「監視公告」功能原本散落在程式目錄下的三份檔案全部改存進 `aniGamer.db`，<br>
並把既有的 `config`／`sn_list`／`manual_single_tasks`／`manual_batch_tasks` 資料表也一併整理成逐欄位存放（不再是一整包難以閱讀的 JSON 字串），方便用 DB 瀏覽器查看與手動修改；<br>
程式內殘留的簡體字（含程式碼註解與使用者可見文字）全面轉換為繁體。<br>
另外修正「檢查更新」訊息顯示原始 Markdown 語法、「掃描集數」特別篇與中文配音集數互相混淆誤植、裝置驗證異常（code=1007）反覆發生、任務頁缺少「公告」按鈕、摺疊區塊箭頭方向與實際展開狀態不符、「操作處置」仍顯示已完成下載公告項目等問題。<br>
共 14 項變更。

<details>
<summary><strong><u>🆕 新功能：公告面板（歷史公告／操作處置／機動調整）（1 項）</u></strong></summary>

導覽列「排程」按鈕右側新增「**公告**」按鈕（設定頁與任務頁皆有），跳出的視窗跟「添加手動任務」一樣的彈窗風格，內含三個可各自自由摺疊、視窗會隨摺疊狀態自動變長變短的區塊：

| 區塊 | 內容 | 好處 |
|---|---|---|
| 歷史公告 | 捲動清單顯示每一則偵測到的公告：時間／劇名（或公告原文，若未比對到追蹤中的作品）／集數（比對追蹤作品目前線上集數推算，延後類公告顯示「集數+1」，公告內有明確指定集數或屬於同時更新則直接採用該數字，無比對資料時顯示「-」）／狀態（暫停更新／提前更新／延後更新／同時更新／暫時下架／無法判斷，依公告內容與該作品上一集網頁上實際顯示的上架時間比對判斷），每 10 秒自動刷新；除「機動調整」外預設摺疊，展開才會顯示內容 | 不用再翻程式日誌或猜測公告內容代表什麼, 一眼能看出每則公告對追蹤中作品的實際影響 |
| 操作處置 | 只列出目標時間尚未過去（或還無法比對到作品、需要人工判斷）**且尚未實際完成下載**的公告；每列顯示公告時間、作品/內容、處置方式下拉選單（等待處置／本週暫停更新／本週提前更新／本週延後更新／本週同時更新／自訂更新時間／自訂連續更新時間／忽略此公告／本週下架）與「確認」按鈕。系統已依判斷結果自動套用「機動調整時間」的預設處置，這裡可以直接覆蓋成別的處置方式；選好後按「確認」才會生效並鎖定，之後系統重新檢查或程式重啟都不會再更動這則公告的處置（只影響這一則公告，不影響未來的公告） | 系統判斷錯誤或想採取不同因應方式時，不需要改程式碼或等下次公告，直接在網頁上蓋過去即可 |
| 機動調整 | 顯示目前生效中的臨時排程調整（暫停檢查／覆蓋成指定時間檢查, 含預期集數與目前已找到集數）與狀態，跟操作處置的資料同步、只是唯讀不含處置操作 | 隨時能確認「機動調整時間」實際上排了哪些調整，跟自己以為的是否一致 |

</details>

<details>
<summary><strong><u>🆕 新功能：每日新番通知（1 項）</u></strong></summary>

「設定」→「通知設定」→「通知類別」新增「每日新番通知」開關（預設開啟），每天 00:00 自動彙整並發送一則通知，內容分兩段：

- **本日排程更新**：依排程清單（`aniGamer.db`，沿用 `sn_list.txt` 格式的自訂星期／時間標籤）列出今天會檢查更新的作品與時間，<br>
並嘗試線上查詢目前實際集數推算「預計播出第 N 話」（查詢失敗時省略集數，不影響整則通知照常發送）
- **機動調整異動**：列出「監視公告」機動調整功能今天生效中的暫停／延期／加更等異動（需開啟「監視公告」與「機動調整時間」才會有內容）

兩段內容都可透過「通知模板」自訂文字與格式（Telegram 可搭配 `<blockquote expandable>` 可摺疊引用標籤，<br>
內容達 4 行以上才會顯示收合箭頭），也可在「通知歷史」查詢過去發送過的每日新番通知內容。<br>
跟其他 10 種通知類別一樣，這是額外新增的第 12 種通知類別，不影響既有類別的開關與模板設定。

</details>

<details>
<summary><strong><u>🆕 新功能：掃描集數全選／反選／取消、Shift+拖曳快速選取（1 項）</u></strong></summary>

手動新增任務「掃描集數」的每個分類（本篇／中文配音／特別篇）各自新增「全選」「反選」「取消」三顆按鈕，只作用在該按鈕所屬分類，不影響其他分類已勾選的狀態；<br>
另外新增 Shift+拖曳快速選取：在任一勾選框上按住 Shift 並按下滑鼠左鍵，<br>
會依這一格目前的狀態決定這次要「勾選」還是「取消勾選」，接著拖曳滑過的每一格都套用同一個結果，放開左鍵即結束。<br>
集數多的長篇作品（例如只想跳過中間某幾集特別篇）不用再逐一手動點擊或全部勾選後再一個個取消。

</details>

<details>
<summary><strong><u>🔧 其他改善：aniGamer.db 資料表全面欄位化整理（1 項）</u></strong></summary>

- 「監視公告」原本散落在程式目錄下的 `gossip_log.jsonl`（公告原文永久紀錄）／`gossip_disposition_log.jsonl`（解析與處置紀錄）／`gossip_pending.json`（生效中的臨時排程）三份檔案，<br>
全部改存進 `aniGamer.db`（新增 `gossip_log`／`gossip_events`／`gossip_pending` 三張表），供上方「公告」面板讀取；<br>
程式啟動時會自動偵測硬碟上是否還有舊版三份檔案並驗證匯入，原始檔案不會自動刪除
- 排程清單（`sn_list.txt` 格式，v25.1.8 起就已改存進 `aniGamer.db`，但當時是整包塞成一個字串欄位）進一步正規化，<br>
改存進新增的 `sn_list_lines` 表，每一行（追蹤項目／單獨的註解行／空白分隔行／星期分類標籤）各自一列存放；<br>
真實使用情境中 `sn_list.txt` 格式大量依賴獨立註解行與空白行來分區整理每個星期／季度追蹤的作品，逐行拆解已用一份 94 行（57 個項目／17 行註解／16 行空白／4 個標籤）的實際檔案驗證來回轉換不會遺漏任何一行；<br>
排程頁面文字編輯器仍以 `sn_list.txt` 格式的原始文字為權威顯示與編輯介面，<br>
磁碟上的 `sn_list.txt` 檔案本身只在從更舊版本升級時用來做一次性匯入，程式不會再實際讀寫這個檔案
- `config`（原本整包 config.json 塞成一個 JSON 字串欄位）改成 `config_kv`，每個設定項目各自一列，<br>
包含 `dashboard`／`ftp`／`coolq_settings`／`notify_categories` 等原本還是巢狀字典的設定也再往下拆一層，每個最底層設定值都各自成一列；<br>
`manual_single_tasks`／`manual_batch_tasks`（原本整包塞一個 `params` JSON 字串欄位）也改成模式／解析度／建立資料夾／執行緒數／彈幕／雙語／重新命名等逐欄位存放
- 整理後每一列資料在 DB 瀏覽器（例如 DB Browser for SQLite）打開時都能直接看懂、直接修改，不會再看到一整條難以閱讀的 JSON blob；<br>
升級時會自動偵測並轉換既有資料，原本的舊版資料表結構不會被覆蓋，轉換失敗時保留舊結構不影響程式運作

</details>

<details>
<summary><strong><u>🔧 其他改善：介面顯示與提示文字微調（1 項）</u></strong></summary>

- 「公告」面板「操作處置」欄位排版調整：處置方式下拉選單欄位縮窄，比對到的作品名稱獨立一行，公告原文換行顯示在下方，取代原本兩者擠在同一窄欄容易看不清楚的排版
- 「取得當前新番更新時間」查詢結果視窗改成兩個各自獨立捲動的清單：「時段不同, 需要確認」與「時段相同,<br>
不需修改」分開放，作品數量多時不用滑過一堆本來就不需要處理的項目才找到真正要看的那幾筆
- 排程檢查日誌「超過自定義時間10分鐘仍未偵測到更新」這行文字顏色由紅字改成綠字——這是預期內、非異常的結果，不該用紅字暗示是錯誤
- 「快速取得」Chat ID 欄位（`col-md-4` 窄欄位放不下完整提示文字）已設定時的遮罩提示文字改用簡短版本，避免文字溢出欄位
- 「通知發送歷史」查詢彈窗的類別／子類別篩選下拉選單改用固定寬度，取代原本自動寬度在選項變動時忽大忽小的問題
- 「暫停更新」公告通知內建模板文字由「《作品名》停止更新」改為「《作品名》本週停止更新」——「監視公告」偵測到的暫停一律只影響「這一週」（下一則公告或下週檢查仍會照常運作），原文字容易讓人誤以為是永久停止追蹤；<br>
此為內建預設模板文字，只有從未在「通知模板」自訂過這則通知、或按過「恢復預設」的使用者會在下次觸發時看到新文字，已自訂內容不受影響

</details>

<details>
<summary><strong><u>🔧 其他改善：全面簡體字轉繁體字（1 項）</u></strong></summary>

- 掃描並轉換 Dashboard 各模板／JS 檔案與 Python 後端程式碼中殘留的簡體字（含程式碼註解與使用者可見文字），例如「模态框」→「模態框」、「关闭」→「關閉」、「用户」→「用戶」、「设置」→「設置」、「并发」→「並發」、「失败」→「失敗」等
- 採用字元對字元的轉換表，刻意不採用會連帶把已經正確的繁體用詞替換成台灣用語習慣的版本（例如「添加」改「新增」、「項目」改「專案」等不在此次調整範圍內），<br>
並人工排除三個容易誤轉的個案：「才」「只」在本專案中的用法不適合轉成「纔」「隻」；<br>
「控制台」的「台」是台灣地區已通用的正確寫法，不需轉成「臺」或「檯」；<br>
另外額外抓到並修正一處合併影片片段語境下「合并」應轉換成「合併」而非直譯的「合並」

</details>

<details>
<summary><strong><u>🐛 BUG 修正：檢查更新訊息顯示原始 Markdown 語法（1 項）</u></strong></summary>

- 修正「檢查更新」偵測到新版本時，GitHub release notes 內容（標題 `##`、粗體 `**文字**`、表格、`<details>` 摺疊區塊等 GitHub-flavored-Markdown 語法）在 CMD 主控台跟 Telegram／Discord 通知裡都是原始語法符號照樣印出來，<br>
而不是真的被渲染，反而比原始文字更難讀；<br>
現在會先轉成乾淨的純文字再輸出，CMD／Telegram／Discord 都適用同一份結果

</details>

<details>
<summary><strong><u>🐛 BUG 修正：解析集數清單時「特別篇」與「中文配音」互相混淆（1 項）</u></strong></summary>

- 修正解析作品集數清單（供「掃描集數」與 `all`／`range` 下載模式使用）時，只要「特別篇」與「中文配音」的集數數量不一致（例如特別篇只有 1 集、中文配音卻有跟本篇一樣多集，這是常見情況），<br>
數量較少的分類用完後，數量較多的分類後續集數會全部被誤標成前一個分類的名稱，<br>
例如中文配音第 2 集以後全部被誤標成「特別篇2」「特別篇3」……導致「特別篇」清單異常膨脹、「中文配音」清單只剩 1 集；<br>
現在改成直接依網頁上每個集數連結實際隸屬的分類標籤判斷，不再用「這個集數編號出現第幾次」反推分類

</details>

<details>
<summary><strong><u>🐛 BUG 修正：公告狀態誤判提前/延後、系統自動處置誤顯示為已手動確認、機動調整未重試即判定過期、跨日時間解析成過去式（1 項）</u></strong></summary>

- 修正判斷公告「提前更新」／「延後更新」時，比對基準誤用資料庫裡的下載完成時間（受下載排隊/分段下載耗時影響，<br>
可能比實際上架時間晚上一兩個小時甚至更多），導致實際上是延後的公告被誤判成提前更新；<br>
改成實際查詢該作品上一集網頁上顯示的「上架時間」當比對基準
- 修正「機動調整時間」開啟時系統自動套用的預設處置，會被畫面誤顯示成「已手動確認」徽章，跟使用者自己在「操作處置」按下確認的結果無法區分
- 修正「操作處置」單一集數的覆蓋檢查時間（提前/延後/自訂更新時間）到了目標時間檢查一次沒找到就直接標記過期、不會重試，<br>
導致公告內容時間沒有完全精準對上實際上架時間時，該筆紀錄會提早從「機動調整」消失；<br>
改成統一給 2 小時重試視窗（每 30 分鐘複查一次），跟「同時更新」「自訂連續更新時間」一致
- 修正公告沒有明確寫出日期、只寫時間時（例如「今晚調整為 02:00 更新」），一律套用「今天」的日期去解析，若公告是在傍晚/晚上發出、公告內容講的又是凌晨時段，<br>
套用「今天」算出來的目標時間其實已經是過去式（中文語感的「今晚」涵蓋到隔天凌晨，但時鐘時間已經跨過午夜），導致覆蓋排程一到點檢查就直接判定過期；<br>
現在改成算出來的目標時間若早於公告發布時間，自動順延一天（更新時間不可能比公告時間還早）

</details>

<details>
<summary><strong><u>🐛 BUG 修正：裝置驗證異常（code=1007）在 cookie 每日失效環境下反覆發生（1 項）</u></strong></summary>

- 裝置ID原本在整個程式執行期間持久共用一份（避免同一帳號短時間內申請大量不同裝置觸發異常判定），只在程式重新啟動時才會檢查 cookie/指紋是否已變更、需不需要換一組新的；<br>
若 cookie 每天都會自動失效重新整理（不少使用者的實際環境），執行期間中途換過的新 cookie 會跟還沒重啟、仍沿用舊 cookie 底下核發的裝置ID對不上，<br>
導致下載一開始就收到 code=1007 裝置驗證異常，且要等到下一次排程檢查週期才會清除重試，體感上像是「每次下載都碰到」；<br>
現在改成每次下載（每一集）開始前都先作廢舊裝置ID、換一組全新的再下載，確保裝置ID永遠對應目前的 cookie

</details>

<details>
<summary><strong><u>🐛 BUG 修正：任務頁缺少「公告」按鈕（1 項）</u></strong></summary>

- 「公告」面板原本只接在「設定」頁的導覽列，「任務」頁（`monitor.html`）的導覽列一直沒有同步補上這顆按鈕，導致停留在任務頁時查看公告還得先切回設定頁；<br>
現已在兩個頁面的導覽列同步加上「公告」按鈕，並共用同一套 JS 邏輯

</details>

<details>
<summary><strong><u>🐛 BUG 修正：摺疊區塊箭頭方向與實際展開狀態不符（1 項）</u></strong></summary>

- 「設定」頁與「公告」面板多處預設收合的摺疊區塊（環境偽裝設定／代理設定／通知設定／Telegram通知／Discord通知／通知類別／通知模板／其他／歷史公告／操作處置等），<br>
原始網頁 HTML 沒有正確標示初始的收合狀態（缺少 `collapsed` class 與 `aria-expanded="false"`），<br>
導致頁面剛載入、使用者還沒點擊過任何標題前，箭頭圖示看起來像是「展開」方向，但底下內容其實是收合隱藏的，兩者顯示不一致容易誤以為頁面故障或內容消失；<br>
點擊過一次後 Bootstrap 會自動修正該次的箭頭方向，所以只有「頁面剛載入、尚未互動」的瞬間會看到這個問題。<br>
現已補上正確的初始 class 與屬性，收合/展開狀態預設就與箭頭方向一致

</details>

<details>
<summary><strong><u>🐛 BUG 修正：「操作處置」仍顯示已完成下載的公告項目（1 項）</u></strong></summary>

- 修正「操作處置」清單原本只依「目標時間是否已過去」篩選要顯示的公告，沒有排除該公告要求的更新其實已經抓到並下載完成的項目（對應的機動調整臨時排程狀態已是「完成」）；<br>
這種情況不需要使用者再確認處置方式，現在會一併檢查機動調整的最新狀態，已完成下載的項目自動從「操作處置」清單移除（仍保留在「歷史公告」的永久紀錄裡）

</details>

<details>
<summary><strong><u>📦 版本（1 項）</u></strong></summary>

- 版本號由 v25.1.9 調整為 v25.2.0

</details>

---

> 完整變更歷史（v24.6 → v25.1.9）請參閱先前的 release notes。
>
> 如果喜歡的可以按下 Star 來表示喜歡<br>
> 可以給予咖啡表示支持<br>
> <a href="https://www.buymeacoffee.com/tocsh" target="_blank"><img src="https://cdn.buymeacoffee.com/buttons/v2/default-blue.png" alt="Buy SH A Coffee" height="36" width="120"></a>
