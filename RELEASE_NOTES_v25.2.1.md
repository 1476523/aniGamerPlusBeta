## v25.2.1

延續 v25.2.0「公告」面板與「每日新番通知」上線後第一批實測回饋，本次除了修正劇場版檔名殘留 `[電影]` 標記、<br>
「每日新番通知」統計範圍改採動畫瘋「本日更新」慣例、公告標題含英數字季別縮寫比對失敗、<br>
「操作處置」被「歷史公告」定期刷新蓋掉操作到一半的下拉選單等既有問題外，<br>
也針對設定頁操作體驗做了較大範圍的調整：新增 aniGamerPlus 專屬的「登入設定」（不用再編輯設定檔）、<br>
目錄瀏覽器可儲存常用的「喜好路徑」、下載目錄／暫存目錄連不到網路磁碟時會主動提示並提供處理選項、<br>
版本更新檢查全面改版（支援預告版本、頁面彈窗、背景定期複查）、快速取得 Cookie 時可自動偵測登入成功，<br>
以及「重新檢查排程更新」改為可先確認清單、逐項選擇下載或標記為已下載。<br>
共 16 項變更。

<details>
<summary><strong><u>🆕 新功能（7 項）</u></strong></summary>

<details>
<summary><strong>「操作處置」選擇指定排程</strong></summary>

「操作處置」清單中，公告內容無法自動比對到任何追蹤作品（狀態「無法判斷」、系統搜尋不到或找不到 sn 交集）的項目，<br>
現在會在作品名稱欄位下方多出一個「選擇指定排程」下拉選單，列出目前追蹤清單（`sn_list.txt`）裡的所有作品供直接挑選；<br>
選好之後跟處置方式一起按「確認」送出即可套用（只有「等待處置」／「忽略此公告」兩種處置方式不需要先選）。<br>
此功能只影響「無法判斷」且尚未自動比對到作品的項目，已經自動比對成功的公告不會顯示這個選單，也不能用它覆蓋掉自動比對結果。

</details>

<details>
<summary><strong>aniGamerPlus 登入設定</strong></summary>

「設定」→「其他」新增「**aniGamerPlus登入設定**」按鈕，跳出的視窗只有帳號／密碼兩個欄位<br>
（不像「動畫瘋登入設定」需要處理 2FA／加密儲存，因為這裡就只是明文的 `dashboard.username`／`dashboard.password`，跟這兩個欄位原本的儲存方式一致），<br>
填寫後按「確認」即可更新登入本控制台需要用的帳號密碼。<br>
過去這兩個欄位只能直接編輯設定檔、網頁上沒有對應的表單欄位；改用網頁操作後不用重啟程式，下次登入即可套用新帳密（目前已登入的狀態不受影響，不會被強制登出）。

</details>

<details>
<summary><strong>目錄瀏覽器新增「喜好路徑」捷徑</strong></summary>

「瀏覽...」目錄瀏覽器最上層（跟「桌面」「下載」與各磁碟機同一層）現在也會列出使用者自訂儲存過的「喜好路徑」（🩷 圖示），<br>
清單最下方固定有一列「🩷 新增路徑」，可把目前路徑輸入框裡打的字串存成喜好路徑（新增項目一律接在既有喜好路徑後面，不會讓「新增路徑」這一列的位置亂跳）；<br>
每個已儲存的喜好路徑列尾端有 🗑️ 按鈕可個別移除，存取都直接透過專屬 API 生效，不需要另外按頁面的「保存」。<br>
儲存時刻意不檢查路徑是否存在／連得到——這個功能主要是給沒有磁碟機代號可以逐層瀏覽的網路路徑（UNC 路徑，例如 NAS）加書籤用，<br>
這類路徑常常會暫時離線，若儲存時就檢查連線性，離線中的 NAS 路徑就永遠存不進去，達不到「先存起來，等連線恢復再點」的目的。

</details>

<details>
<summary><strong>下載目錄／暫存目錄無法存取時的處理與通知</strong></summary>

下載目錄／暫存目錄設定成網路磁碟（UNC 路徑）時，若儲存設定當下、或 NAS 剛好斷線／關機導致連不到，<br>
過去除了主控台一行訊息外完全沒有任何提示，程式會悄悄改成把檔案下載到本機的預設資料夾，使用者可能好幾天都不會發現下載的番劇其實沒有存到原本設定的位置。<br>
現在設定頁最上方會出現黃色警示條，說明是哪個路徑目前連不到、暫時改用了本機哪個資料夾，並提供「處理」按鈕開啟選項視窗：

| 選項 | 行為 |
|---|---|
| 前往修改路徑設定 | 展開下載設定區塊並捲動聚焦到對應輸入框，讓你自己確認並修正路徑 |
| 暫時改用其他路徑 | 開啟目錄瀏覽器另外選一個路徑，選好後仍要按「保存」才會覆蓋掉原設定（不會自動改回原本的網路路徑，適合已經確定要換路徑的情況） |
| 暫時改用本機預設資料夾 | 直接清空成使用程式所在資料夾底下的預設 bangumi／temp 資料夾 |

同時新增「路徑無法存取」通知類別（預設開啟，可在「通知類別」關閉），只在連線狀態從「正常」變成「連不到」的那一刻通知一次（同一次離線期間不會重複洗版），恢復連線後才會再次啟用下一次的通知。<br>
另外也修正了這個機制引入時自帶的一個誤判：全新安裝、本機 bangumi／temp 資料夾尚未建立（還沒有任何下載）時，不會被誤報成「路徑無法存取」。

</details>

<details>
<summary><strong>版本更新檢查全面改版</strong></summary>

「檢查最新版本」原本只在程式啟動時檢查一次，且只能查到官方 GitHub 排除掉預告版本（pre-release）後的正式版，網頁上也完全看不到檢查結果。現在：

| 項目 | 說明 |
|---|---|
| 支援預告版本 | 「其他」區塊新增「檢查預告版本」開關（預設開啟），改抓 GitHub `/releases` 完整清單，可以查到尚未正式發布的預告版本 |
| 背景定期複查 | 除了啟動時檢查一次，之後每 10 分鐘會在背景自動再檢查一次，不用重啟程式就能看到新發布的版本 |
| 頁面彈窗 | 偵測到有更新（正式版顯示「發現新版本」、預告版顯示「發現新版本預告」）時，Dashboard 會自動跳出彈窗，列出目前版本／最新版本／完整更新內容，並附「前往 GitHub 下載」連結 |
| 略過此版本通知 | 彈窗裡按下後，只會抑制「這一個」版本 tag 的自動彈窗，不影響之後偵測到更新版本時繼續提示；正式版與預告版的略過紀錄各自獨立 |
| 導覽列按鈕 | 有可用更新時，導覽列（設定頁與任務頁皆有）會出現「有新版本」／「新版本預告」按鈕，關掉彈窗後仍可隨時點擊重新開啟 |

</details>

<details>
<summary><strong>快速取得 Cookie 自動偵測動畫瘋登入成功</strong></summary>

「動畫瘋登入設定」開啟自動代填帳密登入時，過去填完帳密／驗證碼／2FA 後，仍需要使用者自己切換分頁點擊「取得Cookie」按鈕。<br>
現在登入分頁會在背景輪詢目前網址，偵測到動畫瘋完成驗證後導向的官方入口網站首頁（代表已經登入成功）時，<br>
自動把分頁導回動畫瘋網站並觸發跟按「取得Cookie」按鈕完全相同的流程，抓取結果自動填回欄位。<br>
這裡只偵測「驗證完成後被導去哪裡」這個結果，reCAPTCHA／2FA 等驗證過程仍然完全由使用者親自完成，不涉及、也不會嘗試代為處理任何一步驗證；手動點擊「取得Cookie」按鈕的方式依然保留，沒偵測到跳轉時可以自己按。

</details>

<details>
<summary><strong>「重新檢查排程更新」改版</strong></summary>

原本的「重新檢查所有排程更新」找到更新後會直接自動排入下載，現在改名為「**重新檢查排程更新**」並分成兩個階段：

1. **選擇檢查範圍**：可選「檢查所有排程」（sn_list.txt 全部項目）或「檢查指定排程」——跳出勾選清單（含全選／反選／取消，清單顯示每個排程的分類標籤／下載模式／自訂星期時間），只檢查勾選的項目
2. **逐項確認處理方式**：檢查完成後**不會直接下載**，而是列出所有找到的更新，每一項可各自選擇「下載」或「標記為已下載」（不下載，只在資料庫記為已完成，之後的排程檢查也不會再排入），另有「全部下載」「全部標記為已下載」兩顆快速套用按鈕；確認後才會真正執行

這個設計適合「有幾集其實已經用其他管道看過／取得，不想再重複下載，但也不想讓排程檢查一直重複提示」的情境。<br>
若列出確認清單後 30 分鐘內都沒有按下確認，會自動取消整個作業並恢復正常運作，避免忘記處理的分頁一直卡住自動檢查與新任務排入。

</details>

</details>

<details>
<summary><strong><u>🐛 BUG 修正（5 項）</u></strong></summary>

<details>
<summary><strong>劇場版檔名殘留 `[電影]` 標記</strong></summary>

- v25.1.1 原本已修正劇場版檔名不再附加 `[電影]` 集數標記，但後續版本為了讓「未開啟季度子資料夾分類」時，<br>
同一部作品的正篇／中文配音／特別篇／劇場版仍能分別歸進不同資料夾，在番劇名稱裡額外保留了 `[電影]`／`[特別篇]`／`[中文配音]`／`[中文電影]` 這類類型標籤；<br>
但這個保留的名稱同時也被檔名／通知訊息／主控台顯示等所有對外顯示的地方共用，導致劇場版下載完成的檔名與通知又重新出現 `[電影]`。<br>
現在這類類型標籤只保留在資料夾分類需要用到的內部名稱，對外顯示（檔名／通知／FTP／主控台）一律使用不含類型標籤的乾淨名稱

</details>

<details>
<summary><strong>公告標題含英數字季別縮寫（S2／2nd Season 等）比對失敗</strong></summary>

- 修正公告內容使用英數字季別縮寫（例如《轉生變成史萊姆S4》）、但追蹤清單命名採中文季別（例如「關於我轉生變成史萊姆這檔事 第四季」）時，<br>
系統完全比對不到對應作品、只能標記「無法判斷」的問題。原因有兩個：
  - 季別數字偵測用的 `\bS(\d{1,2})\b` 正規表達式在 Python 裡，中文字與英數字之間沒有 `\b` 邊界，<br>
「史萊姆S4」這種中文名稱直接接英文字母季別、中間沒有空格的常見寫法，季別數字 4 完全偵測不到
  - 就算偵測到季別，系統原本直接拿公告原文（含 `S4` 字樣）去動畫瘋站上搜尋，但站上正式作品名一律用中文「第N季」命名，<br>
英數字縮寫幾乎搜不到任何結果
- 現在改成先修正季別偵測的邊界判斷，再將偵測到的英數字季別（`S2`／`2nd Season` 等）轉換成中文「第N季」接在去除縮寫後的基礎名稱後面重新組成搜尋關鍵字<br>
（例如《轉生變成史萊姆S4》會改搜尋「轉生變成史萊姆 第四季」），並在搜尋結果同一季別出現多筆候選時，依年份新到舊逐一嘗試比對追蹤清單，<br>
不再只認排名最前面的那一筆，避免因為排名不是使用者實際追蹤的那部而漏配

</details>

<details>
<summary><strong>「操作處置」被「歷史公告」定期刷新蓋掉操作到一半的下拉選單</strong></summary>

- 「公告」面板開啟後每 10 秒會自動刷新「歷史公告」／「操作處置」／「機動調整」三個區塊，<br>
但「操作處置」的處置方式下拉選單一旦被使用者改動、還沒按下「確認」，就會被下一輪定期刷新整個蓋掉、選擇的內容還原成刷新前的狀態，等於選了等於沒選；<br>
現在改成只有「歷史公告」與「機動調整」維持每 10 秒定期刷新，「操作處置」只在重新開啟「公告」視窗、<br>
或該列按下「確認」成功套用之後才會重新讀取，操作到一半不會再被刷新打斷

</details>

<details>
<summary><strong>write_settings() 誤刪無法連線的網路路徑設定</strong></summary>

- 下載目錄／暫存目錄設定成目前連不到的網路磁碟／UNC 路徑（例如 NAS 剛好關機）時，儲存設定會把這個路徑欄位永久覆蓋成空字串，之後 NAS 恢復連線也救不回來——<br>
原因是原本的還原邏輯只看「正規化後的值是否等於本機預設目錄」，沒辦法分辨「使用者本來就沒設定」跟「有設定，只是暫時連不到」這兩種情況：<br>
暫時連不到時正規化後的值會被換成本機預設路徑，還原步驟就誤判成「本來就是預設值」而清空
- 現在改成額外記住呼叫端原始送來的路徑，只有在原始值真的等於預設目錄（或本來就是空的）時才清空，路徑只是暫時連不上時會保留原始設定值不動<br>
（見上方「下載目錄／暫存目錄無法存取時的處理與通知」的可見化與處理選項）

</details>

<details>
<summary><strong>設定讀取時反斜線被重複跳脫加倍</strong></summary>

- 讀取設定（`__read_settings_file()`）時，套用了一段原本只該用在解析「使用者手動編輯的舊版 config.json」的修正<br>
（把沒跳脫的反斜線加倍，避免 `json.loads` 直接炸掉），但這段修正被誤用到資料庫欄位本身返回的內容<br>
（不管是現行的 `config_kv`、還是 v25.1.8／v25.1.9 的舊版整包欄位，都已經是正確跳脫過的合法 JSON）——<br>
再套用一次同樣的修正只會把反斜線再加倍一次，存回資料庫後下次讀取又再加倍一次
- `bangumi_dir`／`temp_dir` 因為後面還有 `os.path.abspath()` 正規化，多出來的反斜線被悄悄吃掉沒被發現；<br>
但這次新增的「喜好路徑」`custom_dir_shortcuts` 沒有經過路徑正規化，反覆讀寫幾次後路徑裡的反斜線就會完全走樣、看不出原本設定的網路路徑長什麼樣子<br>
（實測踩到的 bug，透過直接對資料庫做讀寫回圈測試才抓到）。現在移除這段誤用的重複跳脫處理，只在真的需要解析舊版手寫 `config.json` 時才套用

</details>

</details>

<details>
<summary><strong><u>🔧 其他改善（3 項）</u></strong></summary>

<details>
<summary><strong>「每日新番通知」統計範圍改採 13:00～隔日12:59</strong></summary>

- 「每日新番通知」原本統計範圍是日曆日 00:00～23:59（依 `now.isoweekday()` 判斷「今天」排定哪些作品），<br>
但動畫瘋多數深夜番實際更新時間落在 23:00～01:00 左右，跨過午夜就會被排除在通知範圍外，得等到隔天才會出現在通知裡；<br>
現在改成比照動畫瘋網站本身「本日更新」的分類方式，以 13:00 為界，統計當日 13:00 到隔日 12:59 這個區間內排定更新的作品與機動調整異動，<br>
深夜/凌晨更新的集數仍算作「今天」的內容，通知當天就能看到完整清單，排序也依實際更新先後（00:00～12:59 視為接在 23:59 之後）呈現

</details>

<details>
<summary><strong>Cookie／JA3／Akamai 儲存後免重啟即可生效</strong></summary>

- 過去在網頁上更換 Cookie／JA3／Akamai 後，必須重新啟動 aniGamerPlus.exe 才會套用新指紋對應的裝置ID，設定頁也有紅字警告提醒這件事；<br>
沒有照做的話，裝置ID與新指紋對不上，很容易再次觸發「裝置驗證異常」（code=1007）
- 現在存檔（`/cookie`／`/uploadConfig`）後會自動清除裝置ID的記憶體快取，下一次下載取用裝置ID時就會重新判斷是否需要跟著換新的<br>
（真的過期才會重新申請，沒過期會繼續沿用），不需要重啟程式；這則過時的「請務必重新啟動」警告文字也一併移除

</details>

<details>
<summary><strong>設定頁版面整理</strong></summary>

| 項目 | 說明 |
|---|---|
| 下載設定摺疊化 | 「下載設定」比照其他區塊改為可摺疊，且「路徑設定」原本獨立的下載目錄／暫存目錄輸入框，整併移到「下載設定」區塊內（檔名後綴欄位下方），不用再多捲動一個獨立區塊 |
| 「其他」按鈕重新排序整合 | 「其他」區塊按鈕統一排列為：aniGamerPlus登入設定／資料庫整頓／取得當前新番更新時間／重新檢查排程更新／讀取其他版本設定檔／匯出設定與排程檔，原本獨立放在版面最下方的「讀取其他版本設定檔」也移進來一起管理 |
| 聊天室ID改為必填 | 移除原本「手動指定Chat ID」開關與 Telegram `getUpdates` 自動偵測備援機制（依賴短時間內的對話紀錄，排程通知這種非即時場景很容易偵測不到、導致通知悄悄失敗）；欄位更名為「聊天室ID」並加寬，一律透過「取得聊天室ID」按鈕主動查詢後填入並保存 |
| 匯出設定確認改用頁面彈窗 | 「匯出設定與排程檔」的執行前確認提示，從瀏覽器原生 `confirm()` 改成跟其他功能一致的頁面內彈窗 |
| 快速取得後自動儲存 | 「快速取得 Cookie／UA／JA3／Akamai」成功填入欄位後，直接自動觸發保存，不用再額外按一次「保存」按鈕（副作用：若在按「快速取得」前有改過其他還沒存的欄位，會一併存進去，這是配合此行為調整的刻意結果） |
| 密集段落改用 `<br>` 換行 | 修正多個彈窗／區塊說明文字過長擠成一行、不易閱讀的問題 |

</details>

</details>

<details>
<summary><strong><u>📦 版本（1 項）</u></strong></summary>

<details>
<summary><strong>版本號由 v25.2.0 調整為 v25.2.1</strong></summary>

- 版本號由 v25.2.0 調整為 v25.2.1

</details>

</details>

---

> 完整變更歷史（v24.6 → v25.2.1）請參閱先前的 release notes。
>
> 如果喜歡的可以按下 Star 來表示喜歡<br>
> 可以給予咖啡表示支持<br>
> <a href="https://www.buymeacoffee.com/tocsh" target="_blank"><img src="https://cdn.buymeacoffee.com/buttons/v2/default-blue.png" alt="Buy SH A Coffee" height="36" width="120"></a>
